# Agents.md

## Contract

- run `npm run format` on completition of a task unless asked not to.
- use `node` built in typescript handling instead of tsx dependendy, ask before installing external dependencies unless prompted to.
